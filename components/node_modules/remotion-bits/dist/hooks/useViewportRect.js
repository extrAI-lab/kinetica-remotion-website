import { useMemo } from 'react';
import { useVideoConfig } from 'remotion';
import { createRect } from "../utils/geometry.js";
/**
 * Returns a Rect representing the current video composition's viewport.
 * Uses useVideoConfig() internally.
 */
export const useViewportRect = () => {
    const { width, height } = useVideoConfig();
    return useMemo(() => {
        return createRect(width, height);
    }, [width, height]);
};
//# sourceMappingURL=useViewportRect.js.map