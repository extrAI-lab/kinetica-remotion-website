import { random } from "remotion";
export const randomFloat = (seed, min, max) => {
    return random(seed) * (max - min) + min;
};
export const randomInt = (seed, min, max) => {
    return Math.floor(random(seed) * (max - min + 1) + min);
};
export const anyElement = (seed, array) => {
    const index = randomInt(seed, 0, array.length - 1);
    return array[index];
};
//# sourceMappingURL=random.js.map